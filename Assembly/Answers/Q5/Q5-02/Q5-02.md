# Question 5.2
## Part I
Item cost is stored at address 0x30, we can see this when the code loads 0x30 into R2 and uses that to fetch the value stored there (item cost) and load that into R1.

## Part II
The final value before total cost gets saved is 0x54.

## Part III
.equ is just a hardcoded value thats given a variable name, when compiled all instances of OverHead just get replaced by the hardcoded value it isnt stored at any memory address and doesnt get put into assembly.

## Part IV
.space is like a variable but you don't set anything to it at initialisation its for use later on like storing data.

## Part V
In this folder

## Part VI
Also in this folder.