{Connor Alecks: 2254441 / Lukáš Hlaváč : 2278081}
{IP = ip}
SigmaA, IP <- IP(Bbus), IP(Abus) + 1
{SigmaA = ip, IP = ip + 1}
SigmaR <- RAM[SigmaA]
{SigmaA = ip, SigmaR = RAM[ip], IP = ip + 1}
IR <- SigmaR(Bbus)
{SigmaA = ip, SigmaR = RAM[ip], IP = ip + 1, IR = RAM[ip]}


{BCC disp when taken}
IP <- IP(Abus) + IR.val(Bbus)
{IP = ip + 1 + disp}
